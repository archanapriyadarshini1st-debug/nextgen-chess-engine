
# Datasets

- `raw/` raw self-play and engine-vs-engine logs
- `cleaned/` deduplicated training samples
- `features/` engineered inputs for experiments
- `labels/` move/value labels and mistake annotations
- `games/` full game logs in JSONL
- `samples/` extracted training records
